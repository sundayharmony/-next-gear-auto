/** z-index for staff modals/sheets — above bottom tab bar (z-91) and More menu (z-90). */
export const STAFF_OVERLAY_Z = "z-[100]";
export const STAFF_OVERLAY_BACKDROP = `${STAFF_OVERLAY_Z} bg-black/50 backdrop-blur-sm`;
export const STAFF_OVERLAY_PANEL = STAFF_OVERLAY_Z;
