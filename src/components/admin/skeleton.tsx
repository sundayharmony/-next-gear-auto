/**
 * Admin Skeleton Components
 * 
 * Re-exports from the unified skeleton component for backwards compatibility.
 * New code should import directly from @/components/ui/skeleton.
 * 
 * @deprecated Import from @/components/ui/skeleton instead
 */
"use client";

export { 
  Skeleton, 
  DashboardSkeleton, 
  ListSkeleton,
  TableSkeleton,
  StatGridSkeleton,
} from "@/components/ui/skeleton";
