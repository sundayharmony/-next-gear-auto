<?xml version="1.0" encoding="utf-8"?>
<!-- Debug only: allow HTTP to emulator host, localhost, and LAN IPs (ngaDebugApiUrl). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
